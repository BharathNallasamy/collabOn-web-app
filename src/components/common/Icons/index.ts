/**
 * Common Icons Export
 * Centralized icon exports using lucide-react
 */

export * from "./CommonIcons";

// Page-specific SVG icons (non-conflicting names only).
// If additional page icons are required, import directly from PageIcons.
export {
    PencilIcon,
    BackArrow,
    InstIcon,
    ChevronDown,
    CheckSmIcon,
    XSmIcon,
    ExportIcon,
} from "./PageIcons";
